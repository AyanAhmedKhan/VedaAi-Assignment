# Mode-B Express backend lives in this monorepo but is deployed elsewhere
# (Railway / Render / Fly). Vercel only builds the Next.js app.
# IMPORTANT: leading slash anchors the pattern to the repo root,
# so src/server/ is NOT excluded.
/server
/docker-compose.yml

# Local-only artifacts
/.next
/node_modules
*.tsbuildinfo
/screenshots
/screenshot.png
